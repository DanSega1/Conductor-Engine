# Test dependencies (not needed in production containers)
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.14
httpx>=0.27          # needed by tests that import opa_client
pydantic>=2.0
pydantic-settings>=2.0
pre-commit>=3.7
ruff>=0.11,<0.12
# API test dependencies — mirrors the [api] optional extra
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
