# Demonstrates: retry behavior on a failing HTTP task.
# Capabilities: http
# Run: cond run examples/retry.yaml
name: Retry on failure
capability: http
input:
  method: GET
  url: http://does-not-exist.conductor.local
max_retries: 2
