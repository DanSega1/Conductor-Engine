{
  "role": "null-event-bus",
  "version": "1.0",
  "description": "No-op event bus — all emit() calls are silently discarded. Default when no event consumer is configured. Mirrors engine/runtime/bus.py::NullEventBus.",
  "engine_ref": "engine/runtime/bus.py::NullEventBus",
  "parent": "event-bus",
  "instructions": [
    "emit() returns immediately without any action. No logging, no I/O, no side effects.",
    "This is the safe default for minimal deployments."
  ]
}
