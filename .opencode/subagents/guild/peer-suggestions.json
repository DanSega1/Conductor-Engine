{
  "role": "peer-suggestions",
  "version": "1.0",
  "description": "Checks the guild knowledge base before task execution. Returns resolution hints and approach adjustments for known failure patterns.",
  "engine_ref": "docs/conductor/roadmap.md#phase-6--guild-layer",
  "parent": "guild",
  "instructions": [
    "Before a task is submitted, compute input_fingerprint and check guild KB for matching failure patterns.",
    "If a known failure pattern matches, return the resolution hint as an input adjustment suggestion.",
    "The suggestion is advisory — the planner or worker can incorporate it or ignore it.",
    "If no matching pattern, return empty (no suggestion)."
  ]
}
