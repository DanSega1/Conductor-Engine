include_builtins: true

# Additive plugin config for the generic Conductor Engine runtime.
# Built-ins ship automatically; this file exists to demonstrate dynamic loading.

# Optional runtime execution controls keyed by capability name.
# execution_controls:
#   http:
#     timeout_seconds: 5
#     min_interval_seconds: 0.25

capabilities: []

# Example optional memU-backed memory capability:
# - import_path: engine.capabilities.memory:MemoryCapability
#   config:
#     provider_type: memu
#     scope:
#       user_id: conductor-engine
#     service_config:
#       storage_backend: inmemory
